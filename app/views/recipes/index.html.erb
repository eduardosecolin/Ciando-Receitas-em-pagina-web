<h1>Receitas</h1>

 <% @recipes.each do |item| %>
   <article>
    <header> 
     <h2><%= item.name %></h2>
    </header>
    <p> 
     <%= item.stuff %>
    </p>
    <p> 
     <%= item.calories %>
    </p>
    <table>
     <tr>
      <th>Modo de Preparo:</th>
       <td><%= item.prepare_mode %></td>
      </tr>
      <th>Custo de Prato:</th>
      <td><%= number_to_currency(item.cost) %></td> 
      </tr>
     </table>
    </article>  
  <%end %>   